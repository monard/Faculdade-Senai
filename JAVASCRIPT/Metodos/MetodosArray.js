
/*let arrayNumero =  [1,2,3];*/
let numero = new Array (1,2,3);
let nome = ["brendo","monard","nascimento", "souza","Solano"]
let idade = [21,31,41]
let juntarMatriz
let maior30 = idade.
for (contador = 0;contador < nome.length;contador++){
    console.log (numero[contador]);
    console.log(nome[contador]);
}
 /* 
 nome.push (): adicionar um elemento no final.
 nome.pop (): remove o ultimo elemento.
 nome.shift (): remove o primeiro elemento.
 noome.unshif (): Subescresve o elemento na primeira possição.
 delete nome[]: deleta um valor na posição desejada, deixando vazio.
 nome.splice(posição,quant.itens deletados,quais itens vão ser adicionados separados por virgulas)
 let juntarMatriz = nome.concat(nome,idade) >> Junção de Matrizes
 let sobrenome = nome.slice(1,3) >> 1º vai aparecer os elementros depois da posição 1 até 2.
 nome.sort(): deixar em ordem alfabetica.
 nome.reverse(): ordem reversa.
 idade.sort(function(a,b){retorn a-b}): deixar os numeros em ordem crescente.
 idade.sort(function(a,b){retorn b-a}): deixar os numeros em ordem decrescente.
 funtion idade(array){ return Math.max.appy(null,array) }:Maior numero de um array.
 funtion idade(array){ return Math.min.appy(null,array) }:Menor numero de um array.
 let idadeMaior30 = idade.filter(filtragem)
 function filtragem(value,idex,array){return value>30} : retornar valores maiores que 30
 
 */
numero.push();
console.log(numero[3]);
numero.pop();
numero.unshift(9);
